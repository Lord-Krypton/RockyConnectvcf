<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>RockyConnectvcf- Owner</title>
  <link rel="stylesheet" href="styles.css" />
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
</head>
<body class="min-h-screen bg-[var(--bg-dark)] text-[var(--text-primary)]">
  <div class="fixed inset-0 overflow-hidden pointer-events-none opacity-60">
    <div class="absolute -top-40 -left-40 w-96 h-96 bg-[var(--glow-purple)] rounded-full blur-3xl animate-float"></div>
    <div class="absolute top-40 -right-40 w-96 h-96 bg-[var(--glow-accent)] rounded-full blur-3xl animate-float delay-1000"></div>
  </div>

  <main class="relative z-10 max-w-5xl mx-auto px-6 py-16">
    <h1 class="text-5xl md:text-6xl font-bold text-center mb-4 text-gradient">
      RockyConnectvcf
    </h1>
    <p class="text-xl text-center text-slate-400 mb-12">Owner Dashboard</p>

    <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3 mb-16">
      <div class="p-8 rounded-2xl card-glass border-glow shadow-glow">
        <h2 class="text-2xl font-semibold mb-6 text-[var(--primary)]">Create New Session</h2>
        <button id="createSession" class="w-full py-5 text-lg font-medium rounded-xl bg-gradient-to-r from-[var(--primary)] to-[#8b5cf6] hover:from-[#6d28d9] hover:to-[#7c3aed] shadow-lg transition transform hover:scale-105">
          🚀 Create Session
        </button>
      </div>
    </div>

    <h2 class="text-3xl font-bold mb-8 text-[var(--primary)]">Your Sessions</h2>
    <div id="sessionsList" class="space-y-6">
      <p class="text-center text-slate-500 py-12" id="noSessions">No sessions yet. Create one above!</p>
    </div>
  </main>

  <script src="index.js"></script>
</body>
</html>
